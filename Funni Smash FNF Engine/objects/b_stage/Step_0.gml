switch(stage){
	case STAGE.STAGE:
	/*var proc = 0
	repeat(3){
		with(stage_asset[proc]){
			x = ogx - camera.x - scroll_factor//x = (ogx - scroll_factor) - camera.x//(ogx - camera.x * scroll_factor) - ogx
			//y = //(proc != 2 ? (ogy - camera.y * scroll_factor) - ogy : y)
		}
		proc += 1
	}*/
	break;
}
/*
if(keyboard_check(ord("O"))){
	battle.player[0].ogx += (keyboard_check(vk_shift) ? -1 : 1)
}
if(keyboard_check(ord("P"))){
	battle.player[0].ogy += (keyboard_check(vk_shift) ? -1 : 1)
}
if(keyboard_check(ord("K"))){
	battle.player[1].ogx += (keyboard_check(vk_shift) ? -1 : 1)
}
if(keyboard_check(ord("L"))){
	battle.player[1].ogy += (keyboard_check(vk_shift) ? -1 : 1)
}*/
